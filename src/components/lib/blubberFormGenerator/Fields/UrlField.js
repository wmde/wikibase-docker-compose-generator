import TextIputFields from '../TextIputFields';

export default class UrlField extends TextIputFields {
	constructor( Field, BindedObject, Generator ) {
		super( Field, BindedObject, Generator );
	}
}
