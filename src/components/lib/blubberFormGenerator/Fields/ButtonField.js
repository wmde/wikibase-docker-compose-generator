import HTMLInputBase from '../HTMLInputBase';

export default class ButtonField extends HTMLInputBase {
	constructor( Field, BindedObject, Generator ) {
		super( Field, BindedObject, Generator );
	}
}
